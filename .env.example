# ==============================================================================
# WellnessAI Environment Configuration
# ==============================================================================
# Copy this file to .env and fill in your values
# NEVER commit .env to version control!

# ==============================================================================
# Local LLM Settings (Primary - No API Required!)
# ==============================================================================

# Choose your local LLM model
# RECOMMENDED FOR CHAT:
#   - tinyllama (default): Best for conversational responses
#
# MEDICAL DOMAIN (for technical health terminology):
#   - biogpt: Microsoft BioGPT - trained on 15M PubMed abstracts
#   - biogpt-medtext: Quantized BioGPT - lightweight, very fast
#   - biomedlm: Stanford BioMedLM - highest accuracy on medical benchmarks
#
# OTHER OPTIONS:
#   - phi2, flan-t5, mistral
#
# Run `python scripts/download_model.py --list` to see all options
WELLNESS_LLM_MODEL=tinyllama

# Enable/disable local LLM (default: true)
# Set to false to use API-only mode
USE_LOCAL_LLM=true

# Preload model on startup for faster first response (default: false)
# Set to true if you have enough RAM and want instant responses
PRELOAD_LLM=false

# ==============================================================================
# Weather API (Recommended)
# ==============================================================================

# WeatherAPI.com API Key (Free tier available)
# Get yours at: https://www.weatherapi.com/
WEATHERAPI_KEY=your_weatherapi_key_here

# Note: If not set, mock weather data will be used

# ==============================================================================
# External AI APIs (Optional Fallback)
# ==============================================================================
# These are NOT required if using the local LLM
# Only set these if you want API fallback when local LLM fails

# OpenAI API Key (GPT-4)
# Get yours at: https://platform.openai.com/api-keys
# OPENAI_API_KEY=your_openai_api_key_here

# Anthropic API Key (Claude)
# Get yours at: https://console.anthropic.com/
# ANTHROPIC_API_KEY=your_anthropic_api_key_here

# ==============================================================================
# Server Configuration
# ==============================================================================

# Server host (default: 0.0.0.0 for all interfaces)
HOST=0.0.0.0

# Server port (default: 8000)
PORT=8000
